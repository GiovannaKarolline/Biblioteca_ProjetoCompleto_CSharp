@if (ViewData["Sucesso"] is not null)
{
    <div class="alert alert-success">
        @ViewData["Sucesso"]
    </div>
}

@if (ViewData["Falha"] is not null)
{
    <div class="alert alert-danger d-inline-flex gap-3">
        @ViewData["Falha"]
    </div>
}